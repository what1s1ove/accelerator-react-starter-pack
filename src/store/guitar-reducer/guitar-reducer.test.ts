// import { makeFakeGuitar } from '../../utils/mocks';
// import { updateGuitars, uploadGuitars } from '../actions';
// import { guitarReducer } from './guitar-reducer';

// const fakeGuitar = makeFakeGuitar();
// const initialFakeGuitars = new Array(15).fill('').map(makeFakeGuitar);


// describe('Reducer: guitarReducer', () => {
//   it('should update guitars', () => {
//     const state = { guitars: [], sortedGuitars: [] };

//     expect(guitarReducer(state, updateGuitars([fakeGuitar]))).toEqual({ guitars: [], sortedGuitars: [fakeGuitar] });
//   });
//   it('should upload initial guitars', () => {
//     const state = { guitars: [], sortedGuitars: [] };

//     expect(guitarReducer(state, uploadGuitars(initialFakeGuitars))).toEqual({ guitars: initialFakeGuitars, sortedGuitars: [] });
//   });
// });

export {

};
